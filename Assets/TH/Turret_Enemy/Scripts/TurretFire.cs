using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SocialPlatforms;

public class TurretFire : MonoBehaviour
{
    //float currentTime;
    //public float createTime = 3;
    //float distance;

    //public GameObject bulletFactory;
    //public Transform firePosition;

    //private Vector3 nowPoint;
    //private Vector3 firePoint;

    //Turret D;

    //void Start()
    //{
    //    D = GameObject.Find("Turret").GetComponent<Turret>();
    //    GameObject target = GameObject.Find("Rock");
    //    nowPoint = target.transform.position;
    //}

    //void Update()
    //{
    //    GameObject target = GameObject.Find("Rock");
    //    nowPoint = target.transform.position;
    //    firePoint = firePosition.transform.position;

    //    distance = Vector3.Distance(firePoint, nowPoint);

    //    if (D.m_range > distance)
    //    {
    //        currentTime += Time.deltaTime;

    //        if (currentTime > createTime)
    //        {

    //            GameObject bullet = Instantiate(bulletFactory);

    //            bullet.transform.position = firePosition.position;

    //            currentTime = 0;

    //        }
    //    }
    //}
}

